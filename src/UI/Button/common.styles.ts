import { StyleSheet } from 'react-native'

export default StyleSheet.create({
  primaryBtn: {
    height: 50,
    borderRadius: 15,
    width: '100%',
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
    marginVertical: 10
  }
});
