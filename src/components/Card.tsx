import React from 'react'
import { StyleSheet, View } from 'react-native'

function Card () {
  return <View style={styles.container} />
}

const styles = StyleSheet.create({
  container: {
    width: 100,
    height: 100,
    backgroundColor: '#5430ff'
  }
})

export default Card
